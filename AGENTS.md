# Agent instructions

- Never use memories. Don't read, write or rely on any persistent agent memory for this project. Durable context belongs in the repo, such as plans in `docs/`.
- Never add `Co-Authored-By` or other agent attribution lines to git commits or pull requests.
- Limit the amount of code comments, only add it when it's necessary and don't reference docs/ or plans.